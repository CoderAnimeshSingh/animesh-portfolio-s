export interface NavItem {
  id: string;
  label: string;
}

export interface SocialLink {
  platform: string;
  url: string;
  icon: string;
}

export interface Description {
  text: string;
  delay: number;
}